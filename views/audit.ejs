<!DOCTYPE html>
<html>
<head>
    <title>Login Audit Log</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <h1>Audit Log</h1>

    <% if (audits.length === 0) { %>
        <p>No login activity recorded yet.</p>
    <% } else { %>
        <table border="1" cellpadding="5">
            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Login Time</th>
                <th>Success?</th>
                <th>IP Address</th>
            </tr>

            <% for (let i = 0; i < audits.length; i++) { 
                let audit = audits[i]; %>
                <tr>
                    <td><%= audit.id %></td>
                    <td><%= audit.username %></td>
                    <td><%= audit.login_time.toLocaleString() %></td>

                    <td>
                        <% if (audit.success) { %>
                             Yes
                        <% } else { %>
                             No
                        <% } %>
                    </td>

                    <td><%= audit.ip_address %></td>
                </tr>
            <% } %>
        </table>
    <% } %>

    <p><a href="/auth/login">Back to Login</a></p>

</body>
</html>
